    <h1>Pages</h1>
    <% if(pages.length > 0){ %>
        <ul>
        <% for(var i=0;i<pages.length;i++){ %>
            <li><a href="#page&amp;view=<%= pages[i].name %>"><%= pages[i].title %></a></li>
            <% } %>
        </ul>
        <% } %>